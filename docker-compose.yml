services:
  app:
    build:
      context: .
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app/backend
      - ./app/backend/node_modules:/app/backend/node_modules
    environment:
      NODE_ENV: production
